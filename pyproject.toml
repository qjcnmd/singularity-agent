[project]
name = "singularity-agent"
version = "0.1.0"
description = "A production-grade local coding agent runtime."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "click>=8.0",
    "httpx>=0.27",
    "platformdirs>=4.2",
    "pydantic>=2.7",
    "rich>=13.7",
    "typer>=0.12",
]

[project.optional-dependencies]
eval = [
    "PyYAML>=6",
]
sandbox = []
devtools = [
    "tiktoken>=0.7",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
]
test = [
    "pytest>=8.0",
]
build = [
    "hatchling",
]
release = [
    "build>=1.2",
    "twine>=5.0",
]

[project.scripts]
singularity-agent = "singularity.cli:main"
sg = "singularity.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
norecursedirs = [
    ".git",
    ".singularity",
    ".mypy_cache",
    ".pytest_cache",
    ".ruff_cache",
    ".test-tmp",
    ".venv",
    "__pycache__",
    "outputs",
    "work",
]

[tool.hatch.build.targets.wheel]
packages = ["src/singularity"]

[build-system]
requires = ["hatchling", "editables>=0.5"]
build-backend = "hatchling.build"
