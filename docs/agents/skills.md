# Skills 与子代理

- 根 AGENTS.md 中的完整 Skill 路由表和读取要求是规范性项目指令；即使运行环境没有内置 Skills 系统，也必须按该表识别并完整读取对应 SKILL.md。本文仅补充委派与生命周期约束，不替代或缩减根文件路由。Skill 不扩大任务范围、授权或外部写入。
- 默认不启用子代理；仅用户或适用项目指令明确授权时使用。委派前冻结目标、revision、文件所有权、禁止触碰范围、依赖、输出物和验收条件；主代理复核结果、diff、调用链、测试和失败路径。
