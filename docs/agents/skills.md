# Skills 与子代理

- 根 AGENTS.md 的「按需读取的项目指令」清单是规范性路由：命中所列场景时必须完整读取对应文档再行动；即使运行环境没有内置 Skills 系统，也按该清单识别并加载对应 SKILL.md。本文仅补充委派与生命周期约束，不替代或缩减根文件路由。Skill 不扩大任务范围、授权或外部写入。
- 默认不启用子代理；仅用户或适用项目指令明确授权时使用。委派前冻结目标、revision、文件所有权、禁止触碰范围、依赖、输出物和验收条件；主代理复核结果、diff、调用链、测试和失败路径。
