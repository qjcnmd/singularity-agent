# Singularity 仓库指令

Singularity 是本地 Rust harness Agent，以webui为主要交互入口；`--print` 与 `--json` 通过同一 Agent 能力服务自动化和评估。

## 任务依据与按需阅读

当前用户要求决定本次任务；[宪章](.specify/memory/constitution.md)记录长期产品方向；对应 Spec Kit 规格定义阶段目标与验收；源码、配置、测试、Git 与可复现运行证明现状；架构文档解释已实现设计。本文件规定仓库执行方式。

- 涉及产品行为、核心结构、规格、计划或实质实现与审查时，先完整读取宪章；同一任务中已读且未变化的材料沿用已有上下文。文字修正、格式或其他不涉及这些决定的小改动，读取相关内容即可。
- 涉及核心收敛或当前工作台实现时，修改源码前读取对应的 `spec.md`、`plan.md` 与 `tasks.md`，确定目标模型与验收。当前工作台规格位于 [specs/003-local-webui-workbench](specs/003-local-webui-workbench/spec.md)；核心收敛依据位于 [specs/001-core-convergence](specs/001-core-convergence/spec.md)。按本次任务和宪章核对适用范围，历史计划只作背景证据。
- 涉及架构、协议、会话、Provider、工具或评估事实时，读取并同步 [docs/singularity.md](docs/singularity.md) 的相关章节。
- 构建、启动或浏览器验收时，使用工作台 [quickstart.md](specs/003-local-webui-workbench/quickstart.md)；安装与发布操作查阅 [docs/INSTALL.md](docs/INSTALL.md)。其他材料按问题定向读取。

## 决策与完成边界

- 普通 Coding Agent 行为采用 Pi 主导的成熟 harness 基线。常规实现、局部整理与验证自行推进；Singularity 特有取舍，以及实质改变范围、风险、成本、权限或长期结构的问题，向用户说明影响和更简单替代方案后确定。
- 已确定的方案授权完成实现、清除直接替代路径、修复本次改动引起的失败、运行相关本地检查和准备可试用环境。持续推进到本次验收成立；遇到需要用户裁决或外部条件的阻断时，先完成不依赖它的工作，再报告具体缺口。
- 审查按共同的所有权或事实源根因归类，覆盖授权范围内的完整同因集合。交付以已确认工作流、重大问题解决情况和运行证据为准；实现落盘或任务勾选本身不代表验收通过。

## 核心结构与复杂度

- 每项能力具有当前消费者、唯一 owner、明确契约和权威状态来源。新建或保留抽象时，说明其归属及如何降低完整目标系统的复杂度。
- 每个事实只保留一个权威表示，每个行为只保留一条活动路径。结构替换在同一交付边界内接入目标路径，删除被替代的类型、字段、持久化键、转换、分支、调用点和测试，并以符号引用与字面量搜索核对残留。
- Pi 是普通 Agent 行为和核心设计的主要参考。按当前工作流、状态所有权和维护成本判断设计，不把参考实现的状态数、文件数或层数作为机械上限。有需求依据的局部差异自行处理；没有当前消费者或真实边界依据的额外状态、同步层、兼容分支和防御机制应收敛，涉及实质产品取舍时再请用户裁决。保留本地控制面的认证与来源校验等实际安全边界。
- 新增产品能力对应已确认工作流或具有明确假设、范围和验收证据的实验。

## 工作台行为与复用

- 工作台交互以 DSH 对应源码和实际行为为参考，结合当前规格中的用户决定；Codex 式轨迹侧栏、指定推理滑块等局部参考仅适用于已确认部分。用户消息、提问回答和批注中的最新决定及限定应同步到当前规格，替换被否定的方案；历史 research、计划或参考产品存在某功能，不构成重新加入它的依据。具体外观数值与功能清单留在规格，不在本文件重复维护。
- 改动控件前核对现有入口与完整操作过程。搜索、帮助、模型设置、关闭按钮、状态提示及禁用条件均须有当前用途；不要因参考其他产品或实现方便增加同等入口、额外步骤或限制。折叠和展开取决于实际隐藏内容；保留用户判断执行状态所需的反馈。
- 左右侧栏、项目与任务列表、工具展示等共享已确认相同的布局、交互和样式规则。手动操作与自动触发调用同一动作；悬停与执行等触发条件接入已有动画机制，已要求一致的速度、宽度等参数由一处定义。修改共同规则时检查所有相关入口，清理平行实现；各侧栏、项目与任务仍保留各自的选择、展开和运行状态。有依据的差异仅在相应使用方表达，不预建通用配置框架。
- Conversation、轨迹与恢复后的历史消费同一套权威执行和持久化事实。思考、工具调用、输出、错误与运行状态须在执行中及时呈现，并按各自语义在完成、切换任务和重载后保持一致；浏览器不另造执行状态或仅靠临时显示补齐缺失事实。模型能力、推理等级和上下文用量从现有模型元数据及执行结果派生，接入已有配置与保存过程。

## 参考与导航

- **Agent 行为与核心结构**：相关决策前检查 `D:\refs\pi` 的具体实现，重点核对交互语义、执行路径和状态所有权。
- **本地 Web 工作台**：涉及产品布局、浏览器连接或本地控制边界时，从工作台 [research.md](specs/003-local-webui-workbench/research.md) 定位 DSH 参考，再检查影响决策的具体源码或运行行为。
- **补充参考**：`D:\refs\codex` 用于可靠任务执行；`D:\refs\grok-build` 用于直接相关的 Rust 或终端实现问题。参考仓库保持只读，采用实现时遵守本仓库许可证。
- 重大架构或交互结论在任务汇报或研究记录中给出实际检查的参考源码文件、行号、采用结构及其与 Singularity 需求的关系；即使参考未改变结论，也说明检查范围。
- 定义、引用、实现、调用影响及跨文件重命名或删除优先使用可用的 Serena 符号工具。首次使用、缓存缺失或大规模结构变化后，在 Serena 可用时运行 `serena project index`；缓存只用于导航，关键事实以当前源码为准。
- 文档、配置和字面量使用 `rg` 与定向读取。Serena 未暴露或报错时，改用可用的符号、LSP 或文本工具继续，关键结论说明证据范围。

## 验证与交付

- 测试放在拥有行为的模块，覆盖可观察契约、非平凡状态转换和关键失败边界；adapter 与客户端验证自身映射及必要的端到端链路。
- 开发中选择最接近改动的检查，失败后修复并重跑受影响项；通过且无新改动或具体疑点时进入交付。
- 纯文档、决策记录或注释改动检查最终内容、链接、事实归属与 `git diff --check`。项目指令文字调整按此执行；改变产品运行时提示词或 Agent 行为则适用下述核心验收。

- 构建嵌入式 WebUI 前，按 quickstart 生成 production 前端资源。浏览器可见行为变化需在真实运行的 production WebUI 验证受影响的键盘、鼠标、流式更新、滚动和视口流程；布局或信息层级变化还需视觉检查。完整工作台交付按当前规格执行全部验收。
- 改变 Agent 执行、模型调用或持久会话语义时，进行覆盖受影响行为的代表性真实模型评估；完整阶段交付执行完整验收。局部呈现和普通文档修改不自动触发全仓检查或正式评估。评估器通过 `singularity --json` 运行，路径、任务及检查步骤见 quickstart；模型与 Provider 按当前规格的最新决定核实，不沿用旧会话配置。真实浏览器长任务检查与正式 evaluator 分别按各自授权执行；已有同目的、范围和成本边界的明确授权时不重复询问，实质变化才补充确认。必要评估未完成时报告相应验收缺口，不阻塞其他独立工作。
- 长期文档和注释直接描述有效行为、对象职责、理由及非显然不变量，保留必要的条件、时序、失败语义、权限、所有权和副作用。注释以直接阅读源码为主，使用纯文本标题、编号和说明，不使用粗体、反引号、Markdown 链接或代码围栏等排版标记；保留示例、通配符和必要路径中的语义符号。默认采用有层次的直陈述式说明：模块注释先讲用途和适用场景，再按核心流程解释关键步骤、对应函数和不变量；函数与局部注释按内容规模说明契约、必要理由和边界，不机械套用完整模板。Rust 公共 API 使用 Rustdoc。注释不写参考项目名、实现对比或修改过程；参考依据、会话过程和临时审查叙事留在研究记录、任务记录与 Git 历史。
