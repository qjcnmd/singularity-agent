# Singularity 仓库指令

## 必读材料与权威顺序

- 开始仓库任务前完整读取 `.specify/memory/constitution.md`。涉及当前核心收敛时，先建立或
  读取对应的 Spec Kit `spec.md`、`plan.md` 与 `tasks.md`，再修改源码。
- 当前用户要求决定本次任务；宪章记录长期产品方向；当前 Spec Kit 规格记录本阶段目标与验收；代码、配置、测试、Git 与可复现运行证明现状；架构文档解释已实现设计；本文件规定执行方式。
- 普通 Coding Agent 行为采用 Pi 主导的成熟 harness 基线。只把 Singularity 特有取舍，以及会改变范围、风险、成本、权限或长期结构的问题交给用户决定。

## 当前产品与最高优先级

Singularity 是以 Rust 实现的本地交互式 Coding Agent。全屏 TUI 是主要产品入口；`--print` 与 `--json` 为自动化和评估提供同一 Agent 能力。

当前最高优先级是全核心集中收敛：围绕已确认的产品工作流，统一 Agent 执行、
Thread/Turn 与 Session、Context/Compaction、Tool、Model/Provider、项目指令与提示词、
事件与输出，以及 CLI/TUI 接入的职责、状态与契约。开始源码重写前，先在当前 Spec Kit
规格中确定完整目标模型和验收证据；实现直接落到单一目标形状，并在同一交付边界内
清除被取代的路径、表示与调用点。

收敛完成必须同时满足：已确认的日常 Coding Agent 工作流通过；每个核心事实有唯一
权威来源，每个行为有唯一实现路径；全部已确认的重大审查发现及其同因集合得到解决；
确定性核心门禁与经用户批准的真实模型评估均通过。

## 架构与实现规则

- 当前源码、测试和文档是调查现状的证据。目标行为与目标结构来自宪章、当前规格、Pi 源码和已验证工程事实。
- 新建或保留模块、类型、字段、枚举、常量、helper、持久化形状或中间层时，明确其当前消费者、唯一 owner、权威状态和相对既有形状的归属。只保留能降低完整目标系统复杂度的抽象。
- **复杂度门槛以主流 harness 为基线**：Pi 是主要参考；对主流 harness 已有对应能力，主流实现的状态数、活动路径、持久记录、同步层、转换和间接层构成默认复杂度上限。Singularity 设计若明显超过该上限，先归类为候选过度设计；任何偏离都必须在实施前向用户说明差异、Singularity 特有需求、证据、用户影响和更简单替代方案，并由用户明确裁决，Agent 不得自行放行。只有用户裁决后，且当前规格或可复现工程证据证明更高复杂度是完成契约所必需时，才能记录为例外；测试数量、文档数量或“未来可能需要”本身不构成理由。
- 一个概念只保留一个权威表示，一个行为只保留一条活动路径。审查发现重影、平行实现、
  重复状态、旁路、无消费者抽象、不可达路径或旧词形时，先定位共同的所有权或事实源
  根因，再覆盖授权范围内的完整同因集合。
- 结构替换在同一交付边界内完成：接入目标路径，删除被取代的类型、字段、键、映射、转换、分支与测试，并以符号引用和字面量搜索核对残留。
- 任务范围是当前用户要求、有效规格及正确完成所需的支撑。新增产品能力必须对应已确认工作流或有验收证据的实验。

## 参考源码

- Pi 源码位于 `D:\refs\pi`，是普通行为、Agent 执行、状态所有权和核心结构的主要参考。相关决策可能受其影响时，写前检查具体实现。
- Grok Build 源码位于 `D:\refs\grok-build`，用于全屏 TUI 与相关 Rust 实现问题；Codex
  源码位于 `D:\refs\codex`，用于可靠任务执行等直接相关问题。
- 重大架构或交互结论在交付汇报中给出真实的参考源码文件与行号，并说明采用的结构及其与 Singularity 需求的关系。参考未能改变结论时也记录实际检查范围。
- 仓库代码、注释和长期文档直接描述 Singularity 的有效行为、所有权和理由。参考来源与对照证据保留在任务汇报或专门研究记录中。
- 参考仓库保持只读；使用其结构和证据，写入本仓库的实现遵守本仓库许可证与目标设计。

## 代码导航

- 需要定义、引用、实现、调用影响、跨文件重命名或安全删除时，优先使用 Serena 符号工具：
  - 文件符号结构：`get_symbols_overview`
  - 定义与正文：`find_symbol`
  - 引用与调用点：`find_referencing_symbols`
  - trait 或接口实现：`find_implementations`
  - 未知符号名、字符串字面量或配置键：`search_for_pattern`
  - 跨文件重命名与删除：`rename_symbol` / `safe_delete_symbol`
- 文档、配置和纯文本任务使用 `rg` 与定向读取。当前 Agent 未暴露 Serena 工具或 Serena 报错时，使用可用的符号、LSP 或文本工具继续，并在关键结论中说明证据范围。
- Serena 缓存只用于导航，关键事实仍由当前源码、Git 和可复现运行确认。首次使用、缓存缺失或大规模结构变化后运行 `serena project index`。

## 测试与验证

- 测试归属拥有行为的模块，覆盖可观察契约、非平凡状态转换和关键失败边界。adapter 与客户端只测试自身映射或必要的端到端链路。
- 开发过程中运行最接近改动的检查。核心收敛交付执行仓库确定性门禁：

```powershell
cargo fmt --all -- --check
cargo check --workspace --all-targets --all-features --locked
cargo clippy --workspace --all-targets --all-features --locked --no-deps -- -D warnings
cargo test --workspace --all-targets --locked --no-fail-fast
cargo build --workspace --bins --locked
git diff --check
```

- 纯文档、提示词、决策记录或注释改动检查最终内容、链接、事实归属与 `git diff --check`。
- 真实任务评估器位于 `C:\Users\Lenovo\Desktop\Singularity-Evaluator`，通过 `sg --json`
  在隔离工作区执行任务并由 checker 判分。评估任务及 checker 先按当前规格校准；每次
  真实模型评估开始前取得用户明确同意。
- Agent 执行、工具、提示词、上下文、输出、Provider 或相关协议变化时，最终验收同时需要确定性门禁和获批的真实任务评估。评估器解析必须与当前 JSON 事件合同一致。

## 文档与代码注释

- `docs/singularity.md` 解释当前已实现的核心架构；架构、协议、会话、Provider、工具或评估事实变化时同步更新，并以代码和运行证据复核。
- 面向人的文档与代码注释直接描述产品行为、对象职责、设计理由和非显然不变量。Rust 公共 API 使用 Rustdoc。
- 长期文档保留 actor、条件、时序、强制性、失败语义、所有权、副作用和后果；会话过程、临时审查叙事和失效计划留在任务记录与 Git 历史。
