# Singularity 仓库指令

## 项目依据

[宪章](docs/constitution.md)说明产品方向与边界；[架构说明](docs/singularity.md)描述当前实现；[开发指南](docs/development.md)提供运行和检查命令；[安装说明](docs/INSTALL.md)面向安装与发布使用。

当前用户要求和已确认决定确定本次目标。涉及产品方向或边界时查阅宪章；其他工作按问题读取相关源码和文档。源码、配置与可复现运行用于核实现状，历史材料不能自动恢复旧需求或授权。

## 实现与维护

- 先检查相关实现和已有能力，选择完整满足本次目标的简单方案。共同事实和行为由对应模块维护；允许有实际用途的派生视图、缓存和独立实例状态。
- 修改共享机制时检查受影响的使用方，清除直接替代的路径和无消费者内容。允许按可验证的边界分步完成，不为局部问题默认开展全仓收敛；扩大产品行为、数据迁移或重构范围时先说明取舍。
- 工作台、无交互入口和历史恢复复用同一 Agent 执行及持久事实。界面反馈反映真实运行状态；模型能力和用量来自已有元数据与执行结果。项目分组不改变本机工具权限。
- 调整工作台控件前核对其现有入口、完整操作和已确认的用户选择。相同交互复用共同规则，各项目、任务和面板保留各自需要的状态；参考产品存在某功能不构成新增需求。
- 文档在其描述的行为、职责或命令变化时同步。注释说明契约、必要理由及非显然边界，公共 Rust API 使用 Rustdoc；不套用固定篇幅或格式模板。
- 日常工作不要求规格、计划、任务表、审查流水或阶段交接文件。复杂取舍可先在对话中形成简短方案；长期有效内容更新到对应正式文档，不在工具记忆中另存项目规范。

## 参考与导航

Pi 是 Agent 行为的主要参考，DSH 是工作台交互参考。仅在用户要求对齐、语义存在疑问或需要比较设计时检查相关实现；普通修复不以参考源码行号作为开工条件。引用参考得出实质结论时说明来源、采用内容与适用边界。

本机只读参考位置：

- Pi：`D:/refs/pi`。
- DSH：`C:/Users/Lenovo/Documents/Codex/2026-08-14/deepseek-harness-codex-reasoning-effort-ui/work/deepseek-harness`。布局、导航、对话和连接分别位于 `packages/client/ui-layout`、`ui-workspace`、`ui-conversation`、`connection`。
- Codex：`D:/refs/codex`；Grok Build：`D:/refs/grok-build`，用于直接相关的实现问题。

导航可使用现有符号工具或 `rg`；索引在确有导航需要时生成，工具不可用时用其他方式继续。参考仓库保持只读，采用代码时保留所需许可证声明。

## 验证与交付

- 根据变化、失败后果和现有检查的覆盖能力选择验证方式。代码优先运行所属模块的相关检查；测试覆盖可观察行为、非平凡状态转换或关键失败边界，不为凑覆盖率或镜像实现新增测试。
- 页面交互变化在运行的 production WebUI 中检查受影响的操作与状态；布局变化检查实际画面。键盘、鼠标、滚动、流式、刷新和视口场景按影响选择，不机械遍历全部流程。
- 浏览器中的模型试用按用户选定的模型及已确认的目的、范围和成本执行；同范围必要复测不重复询问，实质变化再确认。评估器仅在用户明确要求时运行，不作为开发完成或交付的默认条件。
- 普通文档及仓库指令修改检查最终内容、链接和 `git diff --check`；改变产品运行时提示词按其行为影响验证。CI 与发布按对应工作流执行，不作为每次本地修改的固定清单。
- 完成已授权的实现、直接相关修复和必要验证，并在本地准备可试用版本。交付说明实际结果及重要未验证部分；必要验证受阻时完成其他独立工作并说明缺口，不把未运行或失败的检查计为通过。
