# Singularity 仓库指令

## 项目依据

[宪章](docs/constitution.md)说明产品方向与边界；[架构图谱](docs/architecture.md)用 Mermaid 展示模块关系、状态归属、运行流程与源码入口；[工作台交互](docs/desktop-ui.md)维护控件与显示约定；[开发指南](docs/development.md)提供运行、检查与发布流程；[安装说明](docs/INSTALL.md)面向安装、配置和数据维护。

当前用户要求和已确认决定确定本次目标。涉及产品方向或边界时查阅宪章；其他工作按问题读取相关源码和文档。源码、配置与可复现运行用于核实现状，历史材料不能自动恢复旧需求或授权。

## 实现与维护

- 先检查相关实现和已有能力，选择完整满足本次目标的简单方案。共同事实和行为由对应模块维护；允许有实际用途的派生视图、缓存和独立实例状态。
- 审查复杂机制时，先核对[宪章中的使用边界](docs/constitution.md#运行边界)，再判断实现。已有调用方、测试、历史实现或参考项目不能单独证明需求成立；不以另一套同步、恢复或协调机制重新引入已排除的场景。改变这些边界先与用户讨论。
- 新增状态、抽象、配置、重试或兼容层须对应当前需求或已支持操作下具体的正确性风险；先检查已有机制的保证，只补实际缺口。共同规则在一处维护，被替代路径及无用途的类型、测试、样式和文档一起清理；不以行数目标、机械拆分或转移逻辑代替质量改善。
- 修改共享机制时检查受影响的使用方，清除直接替代的路径和无消费者内容。允许按可验证的边界分步完成，不为局部问题默认开展全仓收敛；扩大产品行为、数据迁移或重构范围时先说明取舍。
- 工作台、无交互入口和历史恢复复用同一 Agent 执行及持久事实。界面反馈反映真实运行状态；模型能力和用量来自已有元数据与执行结果。项目分组不改变本机工具权限。
- 调整工作台控件前核对其现有入口、完整操作和已确认的用户选择。相同交互复用共同规则，各项目、任务和面板保留各自需要的状态；参考产品存在某功能不构成新增需求。
- 文档在其描述的行为、职责或命令变化时同步。注释说明契约、必要理由及非显然边界，公共 Rust API 使用 Rustdoc；不套用固定篇幅或格式模板。
- 日常工作不要求规格、计划、任务表、审查流水或阶段交接文件。复杂取舍可先在对话中形成简短方案；长期有效内容更新到对应正式文档，不在工具记忆中另存项目规范。

## 外部参考

参考其他实现时，先按当前要求确定要采用的具体行为，再逐项核实相关能力；不因采用其中一项就推定需要其余能力。普通修复不以参考源码行号作为开工条件，参考实现也不能单独证明需求成立。

导航可使用现有符号工具或 `rg`；索引在确有导航需要时生成，工具不可用时用其他方式继续。参考仓库保持只读，采用代码时保留所需许可证声明。

## 验证与交付

- 强烈优先从真实工作台、CLI/RPC 入口执行 E2E 测试，并以它作为唯一的测试机制；复杂功能须覆盖完整操作、反馈和持久结果，需要模型参与时调用模型。每次 E2E 结束后生成可核验、可重复的产物，记录前置条件、输入、操作或命令、预期与实测结果，并保留可检查的输出、日志或画面。
- 绝不在编写实现代码后补写单元测试。只有具体的真实故障无法由 E2E 检出时才考虑最少必要的隔离测试；先按该系统当前范围写下可能失败的所有方式及 E2E 遗漏原因，再编写测试与实现代码。现有测试仅保留能检出 E2E 遗漏真实 bug 的用例；接口或格式变化时只维护这些用例的原有保障。具体标准见[开发指南](docs/development.md#测试保留与删减)。
- 页面交互变化在运行的 production Electron 工作台 中检查受影响的操作与状态；布局变化检查实际画面。动效修改检查受影响的展开、收起及状态切换过程，结合实际画面核对方向、位置、连续性和相邻布局。具体检查项及键盘、鼠标、滚动、流式、刷新和视口场景按影响选择，不机械遍历全部流程。
- 完成已授权工作所需的通用模型测试可直接调用，包括工作台试用和同范围必要复测，无需另行请求调用授权。用户指定模型时遵循其选择；否则本机默认使用 `bai/deepseek-v4.1-flash`（B.AI 提供方，已配置在 `~/.singularity/config.json`）。测试按已确认的目的、范围和成本执行，实质扩大范围或成本时再确认。评估器仅在用户明确要求时运行，不作为开发完成或交付的默认条件。
- 普通文档及仓库指令修改检查最终内容、链接和 `git diff --check`；改变产品运行时提示词按其行为影响验证。CI 与发布按对应工作流执行，不作为每次本地修改的固定清单。
- 完成已授权的实现、直接相关修复和必要验证，并在本地准备可试用版本。交付给用户继续试用的工作台默认沿用其当前数据目录与配置；独立数据目录用于隔离验证，需要改变用户试用环境时先说明影响并确认。启动方式遵守数据目录单实例限制，具体步骤见[开发指南](docs/development.md#本地运行)。交付说明实际结果及重要未验证部分；必要验证受阻时完成其他独立工作并说明缺口，不把未运行或失败的检查计为通过。
